# LSTM Full
Original statistical data and neural network models:
- LSTM Full (LSTM + CNN + Attention);
- LSTM Only;
- LSTM + CNN;
- LSTM + Attention;
- LSTM Full (20-Step Input);
- BiLSTM;
- GRU;
- BiGRU;
- TransformerEncoder.
